package cz.cvut.fit.gremlin.sources;

import org.junit.Ignore;
import org.junit.Test;

/**
  * Created by cerva on 11/04/2017.
  */
@Ignore
public class Neo4jTest {

  @Test
  void insertVector() {
/*    val graph = Neo4jGraphResource.getEmptyInstance()
    val label = "name"
    val paris = graph + label
    assert(graph.V(paris.id()).exists())
    assert(graph.V().hasLabel(label).count().equals(1))*/
  }

}
